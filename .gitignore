# build output
/bin/
/dist/
*.exe
*.syso

# go
/vendor/

# web
/web/node_modules/
/web/.vite/

# local runtime data (never commit)
/.jarvis-home*/
*.db
*.db-wal
*.db-shm
/p2-run.*
/ui-*.png

# Application jars are managed artifacts, not source. They are often large and
# frequently carry bundled credentials in application*.yml, so they must never
# land in git history.
*.jar
!/web/**/gradle-wrapper.jar

# editor / os
.DS_Store
Thumbs.db
